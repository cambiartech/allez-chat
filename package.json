{
    "name": "allez-chat",
    "version": "1.0.0",
    "description": "Real-time chat widget for Allez taxi app",
    "scripts": {
        "dev": "netlify dev",
        "build": "cd client && npm run build",
        "start": "netlify dev",
        "install:all": "npm install && cd client && npm install"
    },
    "dependencies": {
        "@netlify/functions": "^2.0.0",
        "mongodb": "^6.0.0",
        "socket.io": "^4.7.0",
        "typescript": "^5.0.0"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "netlify-cli": "^17.0.0"
    }
}
